/**
 * WhatsApp Tool: Customer history
 * Allows the AI agent to retrieve a customer's order history.
 */
export async function getCustomerHistory(customerId: string, limit = 10) {
  // Get recent orders for a customer
  // Return: { orders: [{ number, date, total, status }] }
  throw new Error('Not implemented: connect WhatsApp provider first');
}
