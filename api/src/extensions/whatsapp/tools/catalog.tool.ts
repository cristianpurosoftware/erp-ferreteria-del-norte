/**
 * WhatsApp Tool: Catalog search
 * Allows the AI agent to search and return product information.
 *
 * TODO: Implement when WhatsApp provider is connected.
 */
export async function searchCatalog(query: string) {
  // Search products by name, SKU, or category
  // Return: { products: [{ id, name, sku, price, stock }] }
  throw new Error('Not implemented: connect WhatsApp provider first');
}
