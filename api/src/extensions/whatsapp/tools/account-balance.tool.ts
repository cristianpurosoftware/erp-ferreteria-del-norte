/**
 * WhatsApp Tool: Account balance
 * Allows the AI agent to check a customer's account balance.
 */
export async function getAccountBalance(customerId: string) {
  // Get current balance and overdue amount for a customer
  // Return: { customerId, balance, overdueBalance, creditLimit }
  throw new Error('Not implemented: connect WhatsApp provider first');
}
