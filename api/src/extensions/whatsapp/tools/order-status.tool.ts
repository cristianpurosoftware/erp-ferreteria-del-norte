/**
 * WhatsApp Tool: Order status
 * Allows the AI agent to check the status of an order.
 */
export async function getOrderStatus(orderNumber: number) {
  // Look up order by number and return status + key dates
  // Return: { orderId, number, status, createdAt, estimatedDelivery }
  throw new Error('Not implemented: connect WhatsApp provider first');
}
