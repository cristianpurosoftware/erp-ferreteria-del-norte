/**
 * WhatsApp Tool: Stock check
 * Allows the AI agent to check product availability.
 */
export async function checkStock(productId: string, warehouseId?: string) {
  // Check stock for a product across warehouses
  // Return: { productId, available, reserved, warehouses: [...] }
  throw new Error('Not implemented: connect WhatsApp provider first');
}
