import { EventEmitter } from 'events';

/**
 * Register custom extensions here.
 * This file is in the client customization zone — it is never overwritten by template updates.
 *
 * Example:
 *   import { registerWhatsAppListeners } from './whatsapp/whatsapp.listeners';
 *   eventBus.on('order.created', (order) => { ... });
 */
export function registerExtensions(_eventBus: EventEmitter) {
  // Custom listeners, hooks, and extensions go here
}
