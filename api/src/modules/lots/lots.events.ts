export const LotEvents = {
  CREATED: 'lot.created',
  BLOCKED: 'lot.blocked',
  UNBLOCKED: 'lot.unblocked',
  EXPIRED: 'lot.expired',
  CONSUMED: 'lot.consumed',
  STOCK_LOW: 'stock.low',
  STOCK_EXPIRING: 'stock.expiring',
} as const;
